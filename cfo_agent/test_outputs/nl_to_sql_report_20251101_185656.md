# NL-TO-SQL COMPREHENSIVE TEST REPORT

**Date:** 2025-11-01 18:56:56
**Total Queries:** 53
**Valid SQL:** 53


## Basic Financial

### Q: Show Apple's revenue for Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.revenue / 1e9 as revenue_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: What was Microsoft's net income in Q1 2023?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.net_income / 1e9 as net_income_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 1
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Google's operating income for Q3 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.operating_income / 1e9 as operating_income_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'GOOGL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 3
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Show Amazon's gross profit Q4 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.gross_profit / 1e9 as gross_profit_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AMZN'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 4
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: What is Meta's EPS for Q2 2023?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.eps
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'META'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Multiple Metrics

### Q: Show Apple's revenue, net income, and EPS for Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.revenue / 1e9 as revenue_b,
    cc.net_income / 1e9 as net_income_b,
    cc.eps
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Microsoft's revenue, operating income, and gross profit Q1 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.revenue / 1e9 as revenue_b,
    cc.operating_income / 1e9 as operating_income_b,
    cc.gross_profit / 1e9 as gross_profit_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 1
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Show Google's revenue, margins, and stock price Q3 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.revenue / 1e9 as revenue_b,
    cc.gross_margin * 100 as gross_margin_pct,
    cc.operating_margin * 100 as operating_margin_pct,
    cc.net_margin * 100 as net_margin_pct,
    cc.close_price
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'GOOGL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 3
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Ratios

### Q: What is Apple's gross margin for Q2 2023?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.gross_margin * 100 as gross_margin_pct
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Show Microsoft's operating margin Q1 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.operating_margin * 100 as operating_margin_pct
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 1
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Google's net margin for Q3 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.net_margin * 100 as net_margin_pct
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'GOOGL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 3
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: What is Amazon's ROE in Q4 2023?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.roe * 100 as roe_pct
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AMZN'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 4
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Show Meta's ROA for Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.roa * 100 as roa_pct
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'META'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Apple's debt-to-equity ratio Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.debt_to_equity * 100 as debt_to_equity_pct
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Growth

### Q: Show Apple's revenue growth YoY for Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    g.fiscal_year,
    g.fiscal_quarter,
    g.revenue_yoy_growth * 100 as revenue_yoy_growth_pct
FROM vw_growth_quarter g
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
  AND g.fiscal_year = 2023
  AND g.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: What is Microsoft's QoQ revenue growth in Q1 2023?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    g.fiscal_year,
    g.fiscal_quarter,
    g.revenue_qoq_growth * 100 as revenue_qoq_growth_pct
FROM vw_growth_quarter g
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
  AND g.fiscal_year = 2023
  AND g.fiscal_quarter = 1
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Google's margin growth YoY Q3 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    g.fiscal_year,
    g.fiscal_quarter,
    g.margin_yoy_growth * 100 as margin_yoy_growth_pct
FROM vw_growth_quarter g
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'GOOGL'
  AND g.fiscal_year = 2023
  AND g.fiscal_quarter = 3
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Show Apple's revenue growth for the last 4 quarters

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    g.fiscal_year,
    g.fiscal_quarter,
    g.revenue_yoy_growth * 100 as revenue_yoy_growth_pct
FROM vw_growth_quarter g
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
ORDER BY g.fiscal_year DESC, g.fiscal_quarter DESC
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Stock

### Q: What is Apple's stock price for Q2 2023?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.close_price
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Show Microsoft's stock returns Q1 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.return_qoq * 100 as return_qoq_pct,
    cc.return_yoy * 100 as return_yoy_pct
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 1
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Google's stock volatility for Q3 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.volatility_pct
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'GOOGL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 3
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: What is Amazon's dividend yield Q4 2023?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.dividend_yield * 100 as dividend_yield_pct
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AMZN'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 4
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Macro

### Q: Show Apple's revenue with GDP for Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cmc.fiscal_year,
    cmc.fiscal_quarter,
    cmc.revenue / 1e9 as revenue_b,
    cmc.gdp / 1e12 as gdp_t
FROM vw_company_macro_context_quarter cmc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
  AND cmc.fiscal_year = 2023
  AND cmc.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Microsoft's margins with CPI context Q1 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cmc.fiscal_year,
    cmc.fiscal_quarter,
    cmc.gross_margin * 100 as gross_margin_pct,
    cmc.operating_margin * 100 as operating_margin_pct,
    cmc.net_margin * 100 as net_margin_pct,
    cmc.cpi
FROM vw_company_macro_context_quarter cmc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
  AND cmc.fiscal_year = 2023
  AND cmc.fiscal_quarter = 1
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Show Google's performance with unemployment rate Q3 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cmc.fiscal_year,
    cmc.fiscal_quarter,
    cmc.revenue / 1e9 as revenue_b,
    cmc.net_income / 1e9 as net_income_b,
    cmc.gross_margin * 100 as gross_margin_pct,
    cmc.unemployment_rate
FROM vw_company_macro_context_quarter cmc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'GOOGL'
  AND cmc.fiscal_year = 2023
  AND cmc.fiscal_quarter = 3
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: What is Amazon's revenue with interest rates Q4 2023?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.revenue / 1e9 as revenue_b,
    cmc.fed_funds_rate
FROM vw_company_macro_context_quarter cmc
JOIN dim_company c USING (company_id)
JOIN vw_company_complete_quarter cc USING (company_id, fiscal_year, fiscal_quarter)
WHERE c.ticker = 'AMZN'
  AND cmc.fiscal_year = 2023
  AND cmc.fiscal_quarter = 4
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Sensitivity

### Q: How sensitive is Apple's margin to CPI?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cf.fiscal_year,
    cf.fiscal_quarter,
    cf.gross_margin * 100 as gross_margin_pct,
    cf.beta_gm_cpi_12q as margin_cpi_beta,
    cf.cpi
FROM vw_company_full_quarter cf
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Show Microsoft's margin correlation with GDP

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cf.fiscal_year,
    cf.fiscal_quarter,
    cf.gross_margin * 100 as gross_margin_pct,
    cf.beta_gm_gdp_12q as margin_gdp_beta,
    cf.gdp / 1e12 as gdp_t
FROM vw_company_full_quarter cf
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Google's sensitivity to interest rates

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cf.fiscal_year,
    cf.fiscal_quarter,
    cf.beta_gm_ffr_12q as gross_margin_interest_rate_beta,
    cf.beta_om_ffr_12q as operating_margin_interest_rate_beta,
    cf.beta_nm_ffr_12q as net_margin_interest_rate_beta,
    cf.fed_funds_rate
FROM vw_company_full_quarter cf
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'GOOGL'
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Peer Comparison

### Q: Compare Apple and Microsoft revenue Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.revenue / 1e9 as revenue_b,
    ps.peer_rank,
    ps.peer_percentile
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
LEFT JOIN vw_peer_stats_quarter ps
    ON cc.company_id = ps.company_id
    AND cc.fiscal_year = ps.fiscal_year
    AND cc.fiscal_quarter = ps.fiscal_quarter
    AND ps.metric_name = 'revenue'
WHERE c.ticker IN ('AAPL', 'MSFT')
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 2
ORDER BY cc.revenue DESC
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Compare gross margins for Apple, Microsoft, and Google Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    ps.fiscal_year,
    ps.fiscal_quarter,
    ps.metric_value * 100 as gross_margin_pct,
    ps.peer_rank,
    ps.peer_percentile
FROM vw_peer_stats_quarter ps
JOIN dim_company c USING (company_id)
WHERE c.ticker IN ('AAPL', 'MSFT', 'GOOGL')
  AND ps.fiscal_year = 2023
  AND ps.fiscal_quarter = 2
  AND ps.metric_name = 'gross_margin'
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Rank companies by ROE in Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    ps.fiscal_year,
    ps.fiscal_quarter,
    ps.metric_value * 100 as roe_pct,
    ps.peer_rank,
    ps.peer_percentile
FROM vw_peer_stats_quarter ps
JOIN dim_company c USING (company_id)
WHERE ps.fiscal_year = 2023
  AND ps.fiscal_quarter = 2
  AND ps.metric_name = 'roe'
ORDER BY ps.peer_rank
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Time Series

### Q: Show Apple's revenue for the last 8 quarters

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.revenue / 1e9 as revenue_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
ORDER BY cc.fiscal_year DESC, cc.fiscal_quarter DESC
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Microsoft's margins over the last 2 years

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.gross_margin * 100 as gross_margin_pct,
    cc.operating_margin * 100 as operating_margin_pct,
    cc.net_margin * 100 as net_margin_pct
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
  AND cc.fiscal_year >= (SELECT MAX(fiscal_year) - 2 FROM vw_company_complete_quarter WHERE company_id = c.company_id)
ORDER BY cc.fiscal_year, cc.fiscal_quarter
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Show Google's stock price trend for 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.close_price,
    cc.open_price,
    cc.high_price,
    cc.low_price,
    cc.avg_price
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'GOOGL'
  AND cc.fiscal_year = 2023
ORDER BY cc.fiscal_quarter
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Annual

### Q: Show Apple's annual revenue for 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    ca.fiscal_year,
    ca.revenue_annual / 1e9 as revenue_annual_b
FROM mv_company_complete_annual ca
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
  AND ca.fiscal_year = 2023
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Microsoft's annual margins for 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    ca.fiscal_year,
    ca.gross_margin_annual * 100 as gross_margin_annual_pct,
    ca.operating_margin_annual * 100 as operating_margin_annual_pct,
    ca.net_margin_annual * 100 as net_margin_annual_pct
FROM mv_company_complete_annual ca
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
  AND ca.fiscal_year = 2023
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: What is Google's annual ROE for 2023?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    ca.fiscal_year,
    ca.roe_annual * 100 as roe_annual_pct
FROM mv_company_complete_annual ca
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'GOOGL'
  AND ca.fiscal_year = 2023
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Latest

### Q: Show Apple's latest quarter results

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.revenue / 1e9 as revenue_b,
    cc.net_income / 1e9 as net_income_b,
    cc.gross_margin * 100 as gross_margin_pct,
    cc.operating_margin * 100 as operating_margin_pct,
    cc.close_price
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
JOIN vw_latest_company_quarter lq 
    ON cc.company_id = lq.company_id
    AND cc.fiscal_year = lq.latest_fy
    AND cc.fiscal_quarter = lq.latest_fq
WHERE c.ticker = 'AAPL'
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Microsoft's most recent financial data

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.revenue / 1e9 as revenue_b,
    cc.net_income / 1e9 as net_income_b,
    cc.gross_margin * 100 as gross_margin_pct,
    cc.operating_margin * 100 as operating_margin_pct,
    cc.close_price
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
JOIN vw_latest_company_quarter lq 
    ON cc.company_id = lq.company_id
    AND cc.fiscal_year = lq.latest_fy
    AND cc.fiscal_quarter = lq.latest_fq
WHERE c.ticker = 'MSFT'
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: What is Google's current quarter performance?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.revenue / 1e9 as revenue_b,
    cc.net_income / 1e9 as net_income_b,
    cc.gross_margin * 100 as gross_margin_pct,
    cc.operating_margin * 100 as operating_margin_pct,
    cc.close_price
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
JOIN vw_latest_company_quarter lq 
    ON cc.company_id = lq.company_id
    AND cc.fiscal_year = lq.latest_fy
    AND cc.fiscal_quarter = lq.latest_fq
WHERE c.ticker = 'GOOGL'
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Expenses

### Q: Show Apple's R&D expenses for Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    f.fiscal_year,
    f.fiscal_quarter,
    f.r_and_d_expenses / 1e9 as rnd_expenses_b
FROM fact_financials f
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
  AND f.fiscal_year = 2023
  AND f.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Microsoft's SG&A expenses Q1 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.sg_and_a_expenses / 1e9 as sg_and_a_expenses_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 1
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: What is Google's COGS for Q3 2023?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.cogs / 1e9 as cogs_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'GOOGL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 3
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Show Amazon's R&D and SG&A expenses Q4 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.r_and_d_expenses / 1e9 as r_and_d_expenses_b,
    cc.sg_and_a_expenses / 1e9 as sg_and_a_expenses_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AMZN'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 4
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Cash Flow

### Q: Show Apple's operating cash flow Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.cash_flow_ops / 1e9 as operating_cash_flow_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Microsoft's capex for Q1 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.capex / 1e9 as capex_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 1
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: What is Google's free cash flow Q3 2023?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    (cc.cash_flow_ops - cc.capex) / 1e9 as free_cash_flow_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'GOOGL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 3
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Balance Sheet

### Q: Show Apple's total assets Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.total_assets / 1e9 as total_assets_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Microsoft's total liabilities Q1 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.total_liabilities / 1e9 as total_liabilities_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 1
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: What is Google's equity for Q3 2023?

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.equity / 1e9 as equity_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'GOOGL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 3
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---


## Complex

### Q: Show Apple's revenue, operating income, R&D, stock price, and volatility Q2 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.revenue / 1e9 as revenue_b,
    cc.operating_income / 1e9 as operating_income_b,
    cc.r_and_d_expenses / 1e9 as rnd_expenses_b,
    cc.close_price,
    cc.volatility_pct * 100 as volatility_pct
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'AAPL'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 2
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

### Q: Get Microsoft's complete financial snapshot Q1 2023

**Status:** ✅ VALID

**Generated SQL:**
```sql
SELECT 
    c.ticker,
    cc.fiscal_year,
    cc.fiscal_quarter,
    cc.revenue / 1e9 as revenue_b,
    cc.net_income / 1e9 as net_income_b,
    cc.gross_margin * 100 as gross_margin_pct,
    cc.operating_margin * 100 as operating_margin_pct,
    cc.net_margin * 100 as net_margin_pct,
    cc.eps,
    cc.close_price,
    cc.total_assets / 1e9 as total_assets_b,
    cc.total_liabilities / 1e9 as total_liabilities_b,
    cc.equity / 1e9 as equity_b,
    cc.cash_flow_ops / 1e9 as cash_flow_ops_b,
    cc.capex / 1e9 as capex_b,
    cc.dividends / 1e9 as dividends_b,
    cc.buybacks / 1e9 as buybacks_b
FROM vw_company_complete_quarter cc
JOIN dim_company c USING (company_id)
WHERE c.ticker = 'MSFT'
  AND cc.fiscal_year = 2023
  AND cc.fiscal_quarter = 1
LIMIT :limit;
```

**Parameters:** {'limit': 10}

---

