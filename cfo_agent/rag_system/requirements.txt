# ============================================================================
# RAG SYSTEM - PYTHON DEPENDENCIES (Python 3.9+ Compatible)
# ============================================================================

# Core ML and Embeddings
sentence-transformers==2.2.2        # For generating embeddings (MiniLM-L6-v2)
torch==2.0.1                        # PyTorch (required by sentence-transformers)
transformers==4.30.2                # Hugging Face transformers

# Database
supabase==1.2.0                     # Supabase Python client (compatible with 3.9)
psycopg2-binary==2.9.9              # PostgreSQL adapter (direct connection)
pgvector==0.2.3                     # pgvector Python bindings

# Data Processing
numpy==1.24.3                       # Numerical operations
pandas==2.0.3                       # Data manipulation (3.9 compatible)
tqdm==4.66.1                        # Progress bars

# Environment and Config
python-dotenv==1.0.0                # Load environment variables from .env

# LLM Integration (choose one or both)
openai==1.3.0                       # OpenAI API (GPT-4, GPT-3.5) - 3.9 compatible
anthropic==0.7.0                    # Anthropic API (Claude) - 3.9 compatible

# Optional: LangChain (for advanced RAG orchestration)
# langchain==0.1.0
# langchain-openai==0.0.2

# API and Web Framework (if building REST API)
fastapi==0.104.1                    # Fast web framework (3.9 compatible)
uvicorn==0.24.0                     # ASGI server
pydantic==2.4.2                     # Data validation

# Visualization (for chart generation)
plotly==5.17.0                      # Interactive charts
matplotlib==3.7.3                   # Static charts (3.9 compatible)

# Text Processing
nltk==3.8.1                         # Natural language toolkit
# spacy==3.7.2                      # Skip for now - not essential

# Utilities
pyyaml==6.0.1                       # YAML config files
jsonlines==4.0.0                    # JSON lines format
requests==2.31.0                    # HTTP requests
httpx==0.25.0                       # Async HTTP client

# Testing
pytest==7.4.3                       # Testing framework
pytest-asyncio==0.21.1              # Async testing
pytest-cov==4.1.0                   # Coverage reports

# Logging and Monitoring
loguru==0.7.2                       # Enhanced logging
colorama==0.4.6                     # Colored terminal output

# Development Tools (optional - install separately if needed)
black==23.11.0                      # Code formatting (3.9 compatible)
flake8==6.1.0                       # Linting
mypy==1.7.0                         # Type checking (3.9 compatible)
ipython==8.12.3                     # Interactive shell (latest for 3.9)
# jupyter==1.0.0                    # Skip for now - not essential

# ============================================================================
# INSTALLATION INSTRUCTIONS
# ============================================================================
# 
# 1. Create virtual environment:
#    python -m venv venv
#    source venv/bin/activate  (Mac/Linux)
#    venv\Scripts\activate     (Windows)
#
# 2. Install dependencies:
#    pip install -r requirements.txt
#
# 3. Download spaCy model (if using spaCy):
#    python -m spacy download en_core_web_sm
#
# 4. Download NLTK data (if using NLTK):
#    python -c "import nltk; nltk.download('punkt'); nltk.download('stopwords')"
#
# ============================================================================
# OPTIONAL: GPU Support
# ============================================================================
# 
# For GPU acceleration (NVIDIA CUDA):
# Replace torch line above with:
# torch==2.0.1+cu118 --index-url https://download.pytorch.org/whl/cu118
#
# ============================================================================
